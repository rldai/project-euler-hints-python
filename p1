# PROBLEM:
# If we list all the natural numbers below 10 that are multiples of 3 or 5, we get 3, 5, 6 and 9. The sum of these multiples is 23.
# Find the sum of all the multiples of 3 or 5 below 1000.

# SOLUTION:

def sum_of_multiples(factor, upper_limit):
    number_of_multiples = upper_limit // factor
    print(number_of_multiples)
    return factor * number_of_multiples * (number_of_multiples + 1) / 2

def sum_of_two_multiples(factor1, factor2, upper_limit):
    return sum_of_multiples(factor1, upper_limit) + sum_of_multiples(factor2, upper_limit) - sum_of_multiples(factor1 * factor2, upper_limit)

print(sum_of_two_multiples(3, 5, 999))
